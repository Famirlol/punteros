#include <iostream>
using namespace std;
int main(){
    /*
    //Puntero: variable que almacena direcciones de memoria
    int x=10;
    //declaracion:TIPO *NOMBRE_PUNTERO;
    int *ptr=&x;
    //operadores
    //operador &(operador de direccion):nos sirve para extraer o conocer la direccion de memoria de dicha variable u objeto
    cout <<&x<<endl;//imprime la direccion de memoria de la variable
    cout<<ptr<<endl;
    cout<<*ptr<<endl;//imprime el valor que se encuentra en esta direccion de memoria
    *ptr=1000;
    cout<<*ptr<<endl;//imprime el nuevo valor que hay en esa direccion
    cout<<x<<endl;//imprime el valor que se guardo gracias a ptr*/
    /*
    double var=3.1416;
    double *ptr= &var;
    cout<<*ptr<<endl;
    string cad="ciencia";
    string *ptr1=&cad;
    cout<<*ptr1<<endl;
    cout<<cad.at(0)<<endl;//imprime lo que hay en esa posicion tomando en cuenta que es una cadena
    cout<<(*ptr1).at(0)<<endl;
    cout<<ptr1->at(0)<<endl;//imprime lo que hay en esa posicion igual lo anterior
    cout<<cad.length()<<endl;//imprime la longitud de la cadena
    cout<<ptr1->length()<<endl;
    cout<<(*ptr1).length()<<endl;//* es el operador de indireccion*/
    
    return 0;
}